export const metadata = {
  title: "Release Notes & Changelog",
  description:
    "See what's new in SayLog. Release notes, feature updates, bug fixes, and improvements to the voice calorie tracker.",
  alternates: {
    canonical: "/release-notes",
  },
};

export default function ReleaseNotesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
